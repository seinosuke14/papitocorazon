import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tuempresa.app',
  appName: 'papitocorazon',
  webDir: 'npm run build'
};

export default config;
